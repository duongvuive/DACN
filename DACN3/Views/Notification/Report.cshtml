@model DACN3.Models.ViewModel.DeviceBroken

@{
    ViewData["Title"] = "Report";
}

<h1>Report</h1>

<div>
    <h4>DeviceBroken</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Image)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Image)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.NameDevice)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.NameDevice)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Classroom)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Classroom)
        </dd>
        <dt class="col-sm-2" >
            @Html.DisplayNameFor(model => model.Amount)
        </dt>
        <div class="form-group">
            <label asp-for="Amount" class="control-label"></label>
            <input id="amountInput" asp-for="Amount" class="form-control" />
            <span asp-validation-for="Amount" class="text-danger"></span>
        </div>
    </dl>
</div>
<div>
    <button onclick="ConfirmFunction()">Xác nhận</button>
</div>

<script>
    function ConfirmFunction() {
        var amountValue = document.getElementById('amountInput').value;
        console.log('amountValue:', amountValue); // Thêm dòng này để kiểm tra giá trị amountValue
        var parsedAmount = parseInt(amountValue);

        fetch('/Notification/Confirm', {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json'
            },
            body: JSON.stringify({ amount: parsedAmount })
        })
            .then(response => response.json())
            .then(data => {
                if (data.success) {
                    alert(data.message);
                } else {
                    alert(data.message);
                }
            })
    }

</script>
@* <script>
    function EditFunction() {
        // Tạo một yêu cầu POST bằng AJAX
        fetch('/Notification/EditDeviceBroken', {
            method: 'GET',
            headers: {
                'Content-Type': 'application/json'
            },
        })
    }
</script> *@